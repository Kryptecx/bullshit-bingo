<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title><%= show %> – Bullshit Bingo</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1><%= show %> – Bingo</h1>
  <a href="/">Zurück zur Auswahl</a>

  <div class="grid">
    <% phrases.forEach((phrase, i) => { %>
      <div class="cell" onclick="this.classList.toggle('marked')">
        <%= phrase %>
      </div>
    <% }) %>
  </div>
</body>

<style>
  body {
    margin: 0;
    padding: 20px;
    font-family: sans-serif;
    background-color: #121212;
    color: EEEEEE;
    text-align: center;
  }

  h1 {
    font-size: 2rem;
    margin-bottom: 20px;
    color: #FFFFFF
  }

  .bingo-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 8px;
    max-width: 95vw;
    margin: 0 auto;
  }

  .bingo-cell {
    background-color: #1E1E1E;
    background-size: cover;
    border: 1px solid #2C2C2C;
    border-radius: 12px;
    padding: 8px;
    font-size: 1.5rem;
    min-height: 70px;
    display: flex;
    align-items: center;
    color: #EEEEEE;
    justify-content: center;
    text-align: center;
    user-select: none;
    transition: background 0.3s ease;
    word-wrap: break-word;
    overflow-wrap: break-word;
  }

  .bingo-cell.active {
    background-color: #4caf50;
    color: white;
  }

  @media (max-width: 768px) {
    .bingo-cell {
      font-size: 0.8rem;
      min-height: 60px;
    }
  }

  @media (max-width: 480px) {
    .bingo-cell {
      font-size: 0.75rem;
      min-height: 50px;
    }
  }
</style>

</html>
